---
const { url, alt } = Astro.props;
---

<img
  src={url}
  alt={alt}
  loading="eager"
  class="fixed top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 pointer-events-none opacity-0"
/>
